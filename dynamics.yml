http:
  routers:
    mi-app:
      rule: "Host(`localhost`) || PathPrefix(`/`)"
      entryPoints:
        - web
      service: mi-app
    
  services:
    mi-app:
      loadBalancer:
        servers:
          - url: "http://mi-app:3000"